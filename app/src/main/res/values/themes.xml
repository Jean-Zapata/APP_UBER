<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Uber_Clone" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Uber_Clone" parent="Base.Theme.Uber_Clone" />
    
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">

        <item name="colorControlNormal" value="@color/while" />
        <item name="colorControlActivated" value="@color/while" />
        <item name="titleTextColor" value="@color/while" />
        <item name="colorControlHighlight" value="@color/while" />
        
    </style>
</resources>